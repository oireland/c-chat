#ifndef SERVER_H
#define SERVER_H

/**
 * @brief Starts the server, waits for clients, and relays messages.
 * * @param port The port number for the server to listen on.
 */
void start_server(int port);

#endif // SERVER_H
